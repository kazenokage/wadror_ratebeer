<h2>List of ratings</h2>
<p>Total of <%= @ratings.count %></p>


<ul>
  <% @ratings.each do |rating| %>
  <li><%= rating %><%= link_to 'Destroy', rating, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>

<%= link_to 'Add new rating', new_rating_path %>

